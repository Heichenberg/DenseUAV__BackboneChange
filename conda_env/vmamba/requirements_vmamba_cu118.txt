# DenseUAV + VMamba runtime for Ubuntu 20.04 / system CUDA 11.8.
# Install with:
#   pip install -r conda_env/vmamba/requirements_vmamba_cu118.txt --default-timeout 1000 --retries 20
#
# The +cu118 PyTorch wheels include the CUDA runtime libraries needed to run
# PyTorch. The system CUDA toolkit at /usr/local/cuda is still needed to compile
# third_party/vmamba/kernels/selective_scan because that build uses nvcc.

--extra-index-url https://download.pytorch.org/whl/cu118

torch==2.0.1+cu118
torchvision==0.15.2+cu118

einops>=0.8.1
fvcore>=0.1.5.post20221221
matplotlib>=3.7,<3.8
ninja>=1.11
numpy>=1.24,<2
opencv-python>=4.8
packaging>=23
pillow>=10
pytorch-metric-learning>=2.0
PyYAML>=6
scikit-learn>=1.3
scipy>=1.10
tensorboardX>=2.6
thop>=0.1.1.post2209072238
timm==0.6.12
tqdm>=4.66
yacs>=0.1.8
