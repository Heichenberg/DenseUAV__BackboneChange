conda create -n denseuav_vmamba python=3.
10 -y
conda activate denseuav_vmamba
python -m pip install --upgrade pip setuptools wheel
pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cu121
 pip install -r requirements.txt 

conda install -c nvidia cuda-nvcc=12.1 cuda-cudart-dev=12.1 cuda-libraries-dev=12.1

sudo apt install -y gcc-12 g++-12

wget https://developer.download.nvidia.com/compute/cuda/12.6.0/local_installers/cuda_12.6.0_560.28.03_linux.run
sudo sh cuda_12.6.0_560.28.03_linux.run

只安装cuda-toolkit
cd ~/GIT_REPO/Compare_Trial/Models/VMamba/kernels/selective_scan

unset CPATH
unset CPLUS_INCLUDE_PATH
unset C_INCLUDE_PATH
unset LIBRARY_PATH

export CUDA_HOME=/usr/local/cuda-12.6
export PATH=$CUDA_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$LD_LIBRARY_PATH

export CC=/usr/bin/gcc-12
export CXX=/usr/bin/g++-12
export CUDAHOSTCXX=/usr/bin/g++-12

rm -rf build *.egg-info

